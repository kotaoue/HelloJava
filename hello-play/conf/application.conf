# https://www.playframework.com/documentation/latest/Configuration

# Secret key - used for cryptographic operations.
# For development only. Generate a secure key for production with: sbt playGenerateSecret
# Set APPLICATION_SECRET environment variable to override.
play.http.secret.key = "local-dev-only-not-for-production"
play.http.secret.key = ${?APPLICATION_SECRET}

play.i18n.langs = [ "ja" ]
